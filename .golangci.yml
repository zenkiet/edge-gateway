version: "2"

run:
  timeout: 5m
  tests: false

linters:
  enable:
    # Error handling
    - errcheck
    - staticcheck
    - ineffassign
    - unused

    # Complexity
    - gocyclo
    - bodyclose
